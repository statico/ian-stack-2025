pre-commit:
  parallel: true
  commands:
    check:
      run: pnpm check:fix
      stage_fixed: true
    types:
      run: pnpm types
      glob: "*.{ts,tsx}"

pre-push:
  parallel: true
  commands:
    check:
      run: pnpm check
    types:
      run: pnpm types

